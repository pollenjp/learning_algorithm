#include <stdio.h>

int main()
{
  int n, x;
  int m[100];
  int sum;

  scanf("%d %d", &n, &x);
  printf("%d %d\n", n, x);

  for (int i = 0; i < n; i++){
    scanf("%d", &m[i]);
    sum = m[i]
    printf("%d\n", m[i]);
  }
  printf("sizeof(int) = %zu byte\n", sizeof(m[0]));


  return 0;
}

